#!/usr/bin/env bash

# sudo pip install --user ansible

# # sudo yum -y update
# sudo yum -y install  nano
# sudo yum -y install wget unzip
# sudo  wget http://wordpress.org/latest.zip
# ## Install php-gd in order to work with images, install plugins etc…
# sudo yum -y install php-gd
# ## If you run yum install mysql on CentOS 7, it is MariaDB that is installed rather than MySQL
# sudo yum -y install mariadb mariadb-server mysql
